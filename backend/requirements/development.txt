-r base.txt
google-generativeai>=0.8

# RAG offline build — utilisés uniquement par `python -m rag.embeddings --build`
# pour générer index.faiss + metadata.pkl à uploader ensuite vers Drive.
# Hors prod : Colab a ses propres copies installées via la cellule 0 du notebook.
fastembed>=0.4
faiss-cpu>=1.8

# Tests
pytest>=8.0
pytest-django>=4.8
pytest-cov>=5.0
factory-boy>=3.3

# Linting
flake8>=7.0
black>=24.0

# Dev
django-debug-toolbar>=4.3
ipython>=8.0
